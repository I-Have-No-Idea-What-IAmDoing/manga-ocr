"""Defines the version of the manga_ocr package.

This module contains a single dunder variable, `__version__`, which is used
by packaging tools and is also exposed at the top level of the package. This
allows users to programmatically check the installed version of `manga_ocr`.
"""

__version__ = "0.1.14"
